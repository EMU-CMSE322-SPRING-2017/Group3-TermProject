<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.casper.userregistration">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_takeoutlauncher"
        android:label="TakeOut"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name=".MainPageActivity"
            android:label="TakeOut">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Login Page" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".CustomerPage" />
        <activity android:name=".CustomersPage" />
        <activity android:name=".RegistrationPage" />
        <activity android:name=".RestaurantLoginActivity" />
        <activity android:name=".RestaurantPageActivity" />
        <activity android:name=".MakeOrderActivity" />
        <activity android:name=".ReceiveOrderActivity"></activity>
    </application>

</manifest>